{
  "name": "wapy-calibration-service",
  "version": "1.0.0",
  "description": "The calibration service for the camera frontend.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Minitour/wapy-calibration-service.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Minitour/wapy-calibration-service/issues"
  },
  "homepage": "https://github.com/Minitour/wapy-calibration-service#readme",
  "dependencies": {
    "bleno": "^0.5.0"
  }
}
